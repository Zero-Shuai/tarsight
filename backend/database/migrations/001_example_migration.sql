-- ============================================================================
-- 示例迁移文件
-- 版本: 001
-- 描述: 示例 - 添加新字段
-- 日期: 2025-12-30
-- ============================================================================

-- 说明: 这是一个迁移文件模板
-- 实际使用时,请根据需要修改

-- ============================================================
-- 步骤 1: 添加新列 (如果不存在)
-- ============================================================

-- 示例: 给test_executions表添加一个新列
-- ALTER TABLE public.test_executions
-- ADD COLUMN IF NOT EXISTS new_column_name VARCHAR(255);

-- ============================================================
-- 步骤 2: 创建新表 (如果需要)
-- ============================================================

-- 示例: 创建一个新表
-- CREATE TABLE IF NOT EXISTS public.new_table (
--     id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
--     name VARCHAR(100) NOT NULL,
--     created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
-- );

-- ============================================================
-- 步骤 3: 添加索引 (如果需要)
-- ============================================================

-- 示例: 为新列添加索引
-- CREATE INDEX IF NOT EXISTS idx_new_column
-- ON public.test_executions(new_column);

-- ============================================================
-- 步骤 4: 数据迁移 (如果需要)
-- ============================================================

-- 示例: 从旧表迁移数据到新表
-- INSERT INTO public.new_table (name)
-- SELECT old_column_name FROM public.old_table;

-- ============================================================
-- 步骤 5: 清理旧结构 (如果需要)
-- ============================================================

-- 示例: 删除旧的列 (谨慎操作)
-- ALTER TABLE public.test_executions
-- DROP COLUMN IF EXISTS old_column_name;

-- ============================================================
-- 完成标记
-- ============================================================

-- 记录迁移历史 (可选)
-- INSERT INTO public.schema_migrations (version, description, applied_at)
-- VALUES ('001', '添加新字段', NOW());

-- 输出完成信息
DO $$
BEGIN
    RAISE NOTICE '迁移 001 执行完成';
END $$;
